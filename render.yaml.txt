services:
  - type: web
    name: yaosu-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn yaosu_api:app --host 0.0.0.0 --port 10000
    plan: free
    envVars: []
